# Ralph Progress Log
Started: Wed Apr  8 22:33:27 MDT 2026
---

## 2026-04-08 22:41 UTC - US-001
- Created placeholder OG image (public/og-image.png, 1200x630, 3.7KB) using Python script
- Extended root layout metadata with openGraph and twitter objects
- Files changed: src/app/layout.tsx, public/og-image.png
- **Learnings for future iterations:**
  - Next.js Metadata API merges page-level metadata with layout metadata — only override what's different
  - OG image URL must be absolute (include full domain) for social platforms to fetch it
  - The git root is the monorepo root, not workspace/projects/next-app/ — pnpm commands run from nested dir
  - Dependencies must be installed in workspace/projects/next-app/ (nested) not the monorepo root
  - Chrome/agent-browser not available in this sandbox — use curl to verify HTML output instead
---

## 2026-04-08 22:43 UTC - US-002
- Extended /roadmap page metadata export with openGraph (title, description, url) and twitter (card, title, description) objects
- OG image inherited from root layout — no override needed
- Files changed: src/app/roadmap/page.tsx
- **Learnings for future iterations:**
  - Next.js merges page-level openGraph with layout-level — only specify overrides (title, description, url)
  - The og:image is automatically inherited from root layout metadata
---

## Codebase Patterns
- Mock `next/font/google` in tests: `vi.mock("next/font/google", () => ({ Geist: () => ({ variable: "--font-geist-sans" }), Geist_Mono: () => ({ variable: "--font-geist-mono" }) }))` — required when importing layout.tsx
- Test metadata by importing the exported `metadata` object directly — no HTTP requests needed
- Test files go in `src/test/` directory, named `*.test.ts` or `*.test.tsx`

---

## 2026-04-08 22:45 UTC - US-003
- Created src/test/metadata.test.ts with 7 tests covering root and roadmap OG metadata
- Tests verify: openGraph.title, description, url, siteName, images dimensions, twitter.card
- Required mocking next/font/google to avoid font initialization errors in vitest
- Files changed: src/test/metadata.test.ts
- **Learnings for future iterations:**
  - Importing layout.tsx in tests requires mocking next/font/google (Geist, Geist_Mono)
  - vi.mock() must be called before the import that triggers the mocked module
---

## 2026-04-08 22:47 UTC - US-FINAL
- Archived Ralph run to .ralph/archives/2026-04-08/og-meta-tags/
- Verified dev server (localhost:3000 → 200) and public URL (ruska.dev → 200)
- Created feature branch feat/5-og-meta-tags, pushed to remote
- Created draft PR #8 to development: https://github.com/ryaneggz/next-postgres-shadcn/pull/8
- CI triggered on agent/next-postgres-shadcn branch — passed all checks
- CI run: https://github.com/ryaneggz/next-postgres-shadcn/actions/runs/24172931711
- **Learnings for future iterations:**
  - CI only triggers on pushes to development and agent/next-postgres-shadcn — feat/* branches don't trigger CI
  - To trigger CI for a feature branch PR, push commits to agent/next-postgres-shadcn as well
---
